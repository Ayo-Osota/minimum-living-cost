const Hero = () => {
    return (
            <section className="hero even-columns container">
                <h1>
                The most affortable place to stay in the san franciso bay area
                </h1>
                <div>
                    <div className="map"></div>
                    <form>
                        <select name="" id="">

                        </select>
                    </form>
                </div>
            </section>
    )
}

export default Hero;